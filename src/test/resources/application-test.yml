spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/freshtrace_test?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 5000ms

rocketmq:
  enabled: false

elasticsearch:
  enabled: false

inventory:
  reconciliation:
    enabled: false

fulfillment:
  auto-confirm:
    enabled: false

admin:
  dashboard:
    cache-refresh-enabled: false

product:
  hot:
    enabled: false
