# 复制为 .env 后填入真实值：copy .env.example .env
# 注意：.env 已被 .gitignore 忽略，禁止提交

# MySQL root 密码
MYSQL_ROOT_PASSWORD=change-me

# JWT 签名密钥（建议 32 字节以上随机串）
JWT_SECRET=change-me-to-a-long-random-secret

# AES 密钥（必须 16 字节，用于身份证号加密）
AES_KEY=change-me-16byte
